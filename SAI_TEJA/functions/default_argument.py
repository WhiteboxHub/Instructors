def person(name,age=18): # here we take the default arguments
    print(name)
    print(age)

person("sai teja")   #if we take the when call the fuction the value override the existing default value and the o/p updated....